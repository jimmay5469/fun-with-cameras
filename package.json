{
  "name": "root",
  "private": true,
  "scripts": {
    "demos": "lerna run --stream --scope demos dev",
    "clean": "npx lerna clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
